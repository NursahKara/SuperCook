/**
 * @format
 */

import {AppRegistry} from 'react-native';
import SendNotification from './src/components/sendNotificationButton';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => SendNotification);
